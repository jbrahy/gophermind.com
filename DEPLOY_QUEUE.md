# DEPLOY_QUEUE — gophermind

Staged deploys only. Nothing here has been shipped. gophermind has no remote/
production deploy pipeline — its "deploy" is a local build of the CLI binary
(`bin/build.sh` builds then launches the interactive TUI). The build step is
recorded below for a human to run; the launch step is intentionally not
automated (it would block on an interactive session).

| Date | Change | Tests | Deploy command (run manually) |
|------|--------|-------|-------------------------------|
| 2026-06-12 | `security/audit-2026-06-12` @ `0a49387` — sandbox hardening: SafeJoin symlink containment (F1), search arg-injection guard (F2), deny-list whitespace bypass (F3) | `go vet` clean; `go test ./...` PASS (all packages) | `go build -o gophermind ./cmd/gophermind` (then `./gophermind` to launch, or `bin/build.sh`) |
| 2026-06-12 | `security/audit-2026-06-12` @ `418a4da` — streaming token usage & cost meter: parse `usage` from non-streaming + final streaming chunk (`stream_options.include_usage`), per-session UsageMeter accumulator with configurable per-1K prices, surfaced in TUI status line and `run`/`ask` stderr | `go vet` clean; `go test ./...` PASS (all packages); `go test -race` clean on agent/tui/llm | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `330470c` — named provider profiles: built-in `local-llama`/`openai`/`anthropic-proxy` plus user-defined profiles via `GOPHERMIND_PROFILE_<NAME>_*` env vars, selectable with `--profile`/`-p` (flag > env > default); per-field resolution (per-profile env > built-in default); legacy single-endpoint behavior unchanged; validated names, no key leakage in errors | `go vet ./...` clean; `go test ./...` PASS (all packages) | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `d3e3471` — automatic retry with backoff: bounded exponential-backoff-with-jitter retries on HTTP 429/5xx and transient network errors for `Complete` and the initial connect of `Stream`; honors (and caps) `Retry-After`; context-aware backoff sleeps; streaming never replays after tokens are emitted; configurable via `GOPHERMIND_MAX_ATTEMPTS`/`GOPHERMIND_RETRY_BASE_DELAY_MS` | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race ./internal/llm` clean | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `fc83a95` — response caching by prompt hash: opt-in content-addressed disk cache for non-streaming `Complete`, keyed by SHA-256 over canonical JSON of (model, messages, tools, temperature, tool_choice); TTL expiry, atomic temp+rename writes (0600 files / 0700 dir), corrupt/oversized files treated as a miss; API key never in key or value; off by default (`GOPHERMIND_CACHE_ENABLED`/`_DIR`/`_TTL`); disabled => zero disk I/O, behavior unchanged | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race ./internal/llm` clean | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `47cd95c` — model capability probe: resolves context window, max output tokens, and tool-calling support per model+endpoint (endpoint-reported `/v1/models` fields > built-in family table > conservative defaults), with a `Source` tag per value; in-memory per-endpoint+model cache so a session never re-probes; never breaks startup (network/parse errors and context cancellation degrade to table/default); untrusted endpoint values clamped (reject <=0 and >10M context/max-output), `/v1/models` read capped via LimitReader (1 MiB), API key/raw bodies never logged | `go vet ./...` clean; `go test ./...` PASS (all packages) | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `75b8eba` — fallback model chain: ordered list (primary `Model` then `GOPHERMIND_FALLBACK_MODELS`, comma-separated/trimmed/deduped/capped at 8) tried in order; on a fallback-eligible failure (after that model's own retries) the chain advances to the next model, returning an aggregate error naming all tried models when exhausted; fallback wraps OUTSIDE retry (attempts bounded by chain x retries, respects ctx deadline); eligibility is status/error-based (network/5xx/429/404-model-not-found fall back; ctx-cancel/401/403/400/2xx-decode are terminal — no NLP refusal detection); cache + usage attributed to the model that answered; streaming falls back only pre-token (never switches models mid-stream / no token replay); empty list => single-model behavior unchanged; aggregate error carries only model names + truncated last error (no key/headers/bodies) | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race ./internal/llm` clean | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `bfec093` — per-request temperature/top-p override: sampling params surfaced through config (`GOPHERMIND_TEMPERATURE` default 0 / range [0,2]; `GOPHERMIND_TOP_P` default unset / range (0,1]) and runtime `/temp <v>` and `/topp <v>` TUI commands; `TopP` added to `ChatRequest` as `*float64` (omitempty) so unset top_p is omitted while temperature is always sent (explicit 0 preserved); mutex-guarded client sampling read per request by `Complete`/`Stream` so a change takes effect on the next request; cache key includes top_p; validators reject NaN/Inf and out-of-range (config + `/temp`/`/topp`), bad input leaves value unchanged without crashing; no new logging of keys/prompts | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race ./internal/llm ./internal/tui ./internal/config ./internal/agent` clean | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `3d76201` — prompt/response transcript export: `--transcript <path>` flag (and `GOPHERMIND_TRANSCRIPT` env) dumps the full wire-level message history as JSONL (one exact `llm.Message` per line — role/content/tool_calls/tool_call_id/name — in order); `agent.ExportJSONL(io.Writer)` streams line-by-line via a buffered `json.Encoder` (embedded newlines escaped; never buffers the whole blob); `agent.WriteTranscript(path)` opens 0600 file / 0700 parent dir and writes; wired into `run`/`ask` (once at session end, even on error) and chat TUI (flush on exit after alt-screen teardown); unset => no file / zero overhead; explicit user output path (NOT repo-contained); security: file 0600 + dir 0700 at rest, only message history serialized (no API key / Authorization / base-URL creds — regression-tested), empty-path rejected, O_CREATE\|O_WRONLY\|O_TRUNC bounds overwrite/symlink surprises, bad path warns without crash | `go vet ./...` clean; `go test ./...` PASS (all packages) | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `e475577` — optional mutual-TLS / custom-CA: secure alternative to `-insecure` for internal endpoints. `GOPHERMIND_CLIENT_CERT` + `GOPHERMIND_CLIENT_KEY` (required together; `-client-cert`/`-client-key`) enable client-certificate auth; `GOPHERMIND_CA_CERT` (`-ca-cert`) trusts a private CA for the server while keeping verification ON (RootCAs = clone of system pool + the CA; missing/garbage CA fails CLOSED, no silent downgrade). New `llm.NewWithTLS`/`TLSOptions` build a validated `tls.Config` (MinVersion TLS 1.2; cert via `tls.LoadX509KeyPair`); bad cert/key/CA fails fast at startup. `New()` preserved byte-for-byte; no options set => prior default transport. Precedence: `-insecure` still skips verify but a configured client cert is still presented. Security: key bytes never logged/in errors (regression-tested), system pool cloned not mutated, empty-pool fallback when SystemCertPool() errors, cert/key/CA paths never reach usage meter/transcript/Stringer | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race ./internal/llm` clean | `go build -o gophermind ./cmd/gophermind` |
| 2026-06-12 | `security/audit-2026-06-12` @ `d5e07de` — request/response middleware hooks: idiomatic `Middleware func(http.RoundTripper) http.RoundTripper` seam registered via `Client.Use(...)`, wrapping the TLS-configured base transport so BOTH `Complete` and `Stream` are covered uniformly; `RequestHook`/`ResponseHook` function adapters (`HookMiddleware`) so callers write a one-liner; built-in `HeaderInjector(name,value)` and redaction-aware `LoggingMiddleware(io.Writer)` (logs method/URL/status/duration only). Index 0 = outermost (request hook runs first). No middleware => base transport used directly, zero wrapping overhead. Security: logging NEVER emits Authorization/Proxy-Authorization/Cookie/Set-Cookie or any token/api-key header value (redacted), NEVER reads request or response bodies (no token/body leak, no SSE buffering — streaming-incremental regression-tested); request-hook error aborts WITHOUT hitting the network, response-hook error fails the request and closes the body; hook panics recovered into transport errors (no process crash); header values with CRLF rejected by net/http (no response-splitting); body integrity preserved via GetBody restore (regression-tested non-empty body) | `go vet ./...` clean; `go test ./...` PASS (all packages); `go test -race` clean on new middleware tests | `go build -o gophermind ./cmd/gophermind` |

No infrastructure / IaC changes in this audit (none exists in this repo).
