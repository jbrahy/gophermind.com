{
  "phase": "5",
  "title": "E2E remote mode: WG tunnel, chat, approve, disconnect",
  "description": "Write E2E tests for remote mode: establish WG tunnel → chat → approve → disconnect. Spawns a real gophermind-server behind a userspace WireGuard server (same pattern as connection_test.go's TestConnection_RemoteMode_ConnectsAndHealthy), then exercises the full client API through the tunnel.",
  "acceptance_criteria": [
    "Remote mode E2E: WG tunnel established → chat → stream → approve → disconnect passes",
    "Tests pass with go test -race -tags e2e ./gophermind-osx/..."
  ],
  "agent": "executor",
  "model": "strong",
  "depends_on": [
    "05-01",
    "03-03",
    "01-02"
  ]
}
