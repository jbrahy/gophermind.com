{
  "phase": "5",
  "title": "E2E model switching: pin, cycle, switch",
  "description": "Write E2E tests for model switching: list models → pin a model → cycle through models → verify switch. Exercises the client's model endpoints (ListModels, Catalogue, ModelSettings, PatchModelSettings) against a real gophermind-server.",
  "acceptance_criteria": [
    "Model switching E2E: list → pin → cycle → switch passes",
    "Tests pass with go test -race -tags e2e ./gophermind-osx/..."
  ],
  "agent": "executor",
  "model": "strong",
  "depends_on": [
    "05-01",
    "04-04"
  ]
}
