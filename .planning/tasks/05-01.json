{
  "phase": "5",
  "title": "E2E local mode: full flow, approval, session management",
  "description": "Write E2E tests for local mode: launch server → create session → chat → stream → approve → complete, plus session management (create → rename → resume → delete). Tests spawn a real gophermind-server subprocess via connection.New + Connect, exercise the client API end-to-end.",
  "acceptance_criteria": [
    "Local mode E2E: launch → create session → chat → stream → approve → complete passes",
    "Session management E2E: create → rename → resume → delete passes",
    "Tests pass with go test -race -tags e2e ./gophermind-osx/..."
  ],
  "agent": "executor",
  "model": "strong",
  "depends_on": [
    "04-01",
    "04-02",
    "04-05",
    "03-02",
    "03-03"
  ],
  "status": "completed",
  "notes": "Done in e2e_local_test.go (build tag e2e). Key API details: CreateSession returns (string, error); session file only written after first turn; EventStream uses Next() not Events(); approval events decoded via Event.ApprovalNeeded()."
}
