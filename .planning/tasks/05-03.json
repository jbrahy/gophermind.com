{
  "phase": "5",
  "title": "E2E pipeline: breakdown, execute, monitor",
  "description": "Write E2E tests for the pipeline flow: pick spec → breakdown → execute → monitor. Spawns a real gophermind-server with a .planning directory, starts a breakdown via the client API, monitors pipeline state and events via SSE, verifies task status transitions.",
  "acceptance_criteria": [
    "Pipeline E2E: start breakdown → monitor state → verify task transitions passes",
    "Tests pass with go test -race -tags e2e ./gophermind-osx/..."
  ],
  "agent": "executor",
  "model": "strong",
  "depends_on": [
    "05-01",
    "04-06",
    "02-04"
  ]
}
